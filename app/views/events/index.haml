%h1 Events

%table.table.table-striped
  %thead
    %tr
      %th Event name
      %th Start date
      %th End date
      %th Actions
  %tbody
    - events.each do |e|
      %tr
        %td= e.name
        %td= e.start_date
        %td= e.end_date
        %td
